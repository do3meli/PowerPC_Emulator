// this function converts a decimal input string into a binary two's complement
function dec2bin(input) {
	
	alert(input.charAt(0));
	if( input.charAt(0) == "-" ){
	
	}else{
		var bin = input.toString(2);
	}
	
	return bin;
}
